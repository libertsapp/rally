# Copie para .env e ajuste. Em dev local, pode deixar tudo comentado —
# o app cai pro SQLite local (./volei.db) automaticamente.

# Postgres em produção (Vercel Postgres / Neon / Supabase) — string de conexão
# completa, algo como:
# DATABASE_URL=postgresql+psycopg://usuario:senha@host:5432/nome_do_banco
